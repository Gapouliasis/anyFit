# Convert sxts to RasterBrick

S3 generic for converting an sxts object to a `RasterBrick`.

## Usage

``` r
rasterFromSxts(x, ...)
```

## Arguments

- x:

  An sxts object.

- ...:

  Additional arguments passed to methods.
