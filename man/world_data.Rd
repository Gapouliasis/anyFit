% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{world_data}
\alias{world_data}
\title{World country and continent boundary polygons}
\format{
A \link[sp:SpatialPolygonsDataFrame]{sp::SpatialPolygonsDataFrame} with 256 features and 8 attribute
columns:
\describe{
\item{iso3}{Three-letter ISO 3166-1 alpha-3 country code.}
\item{status}{Sovereignty or status of the territory.}
\item{color_code}{Colour group used for cartographic display.}
\item{name}{Country or territory name (matched by \verb{country =} arguments).}
\item{continent}{Continent name (matched by \verb{continent =} arguments).}
\item{region}{Sub-regional grouping.}
\item{iso_3166_1_}{ISO 3166-1 numeric code.}
\item{french_shor}{Short country name in French.}
}
}
\source{
Bundled global country/territory boundary polygons.
}
\usage{
world_data
}
\description{
A global map of country and territory boundaries whose attributes back
anyFit's spatial helpers. The \code{name} and \code{continent} columns are the ones
matched when a \code{country} or \code{continent} is supplied to \code{\link[=mask.sxts]{mask.sxts()}} and
\code{\link[=zonal_stats]{zonal_stats()}}, so the exact spelling in these columns is what those
arguments expect (e.g. the United Kingdom is stored as
\code{"U.K. of Great Britain and Northern Ireland"}).
}
\examples{
# Continents available for `continent =` masking
sort(unique(world_data$continent))

# Exact country name used by `country =` arguments
grep("Great Britain", world_data$name, value = TRUE)
}
\seealso{
\code{\link[=mask.sxts]{mask.sxts()}}, \code{\link[=zonal_stats]{zonal_stats()}}
}
\keyword{datasets}
