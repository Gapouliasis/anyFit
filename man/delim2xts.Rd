% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delim2xts.R
\name{delim2xts}
\alias{delim2xts}
\title{delim2xts}
\usage{
delim2xts(
  file_path,
  time_zone,
  date_index = 1,
  strict_step = TRUE,
  delim = "\\t",
  skip_rows = 0,
  col_names = TRUE,
  no_value = NA,
  exc_leaps = TRUE,
  save_Xts = FALSE,
  filename = NA
)
}
\arguments{
\item{file_path}{Character. Path to the delimited data file.}

\item{time_zone}{Character. Time zone for date parsing (e.g. \code{"UTC"}).}

\item{date_index}{Integer. Column position of the date/time variable.
Default \code{1}.}

\item{strict_step}{Logical. If \code{TRUE} (default), gaps are filled
with \code{NA} at the detected regular interval.}

\item{delim}{Character. Field delimiter. Default \code{"\t"}.}

\item{skip_rows}{Integer. Number of header rows to skip. Default \code{0}.}

\item{col_names}{Logical. If \code{TRUE} (default), the first non-skipped
row contains column names.}

\item{no_value}{Value to be treated as missing (\code{NA}). Default
\code{NA}.}

\item{exc_leaps}{Logical. If \code{TRUE} (default), 29 February is
excluded from leap years.}

\item{save_Xts}{Logical. If \code{TRUE}, the xts is saved as a
tab-delimited text file. Default \code{FALSE}.}

\item{filename}{Character. Output filename (without extension) used when
\code{save_Xts = TRUE}. Default \code{NA}.}
}
\value{
An \code{\link[xts]{xts}} object with parsed timestamps as the
index and the remaining columns as the data matrix.
}
\description{
Reads a delimited text file and returns an xts object with
auto-detected timestamps. The date column is parsed via
\code{\link[lubridate]{parse_date_time}} using a comprehensive set of
order formats (ymd, dmy, mdy, and variants with hours, minutes, and
seconds). When \code{strict_step = TRUE}, the function detects the
regular time step from the first difference of parsed dates, constructs
a complete index spanning the full date range, and fills missing
positions with \code{NA} entries. Leap days (29 February) may be
excluded from leap years via \code{exc_leaps}. A sentinel
no-data value may be specified via \code{no_value} and replaced with
\code{NA}. The resulting xts can optionally be saved to a
tab-delimited text file.
}
\examples{
# Create a temporary delimited file
tf <- tempfile(fileext = ".csv")
writeLines(c("date,value", "2020-01-01,10.5", "2020-01-02,12.3",
             "2020-01-03,11.0", "2020-01-04,9.8"), tf)
x <- delim2xts(tf, time_zone = "UTC", delim = ",", strict_step = TRUE)
head(x)

}
