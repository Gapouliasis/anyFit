% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitlm_nc.R
\name{fitlm_nc}
\alias{fitlm_nc}
\title{fitlm_nc}
\usage{
fitlm_nc(
  data = NULL,
  filename = NA,
  varname = NA,
  candidates = "norm",
  ignore_zeros = FALSE,
  zero_threshold = 0.01,
  parallel = FALSE,
  ncores = 2,
  shared_memory = TRUE,
  order = NULL,
  ...
)
}
\arguments{
\item{data}{An sxts object, or a raster file. Leave NULL when supplying filename/varname.}

\item{filename}{(optional) A NetCDF file name to import if data is not provided.}

\item{varname}{(optional) The name of the variable to extract from 'filename'.}

\item{candidates}{A list of distribution to fit.}

\item{ignore_zeros}{A logical value, if TRUE zeros will be ignored. Default is FALSE.}

\item{zero_threshold}{The threshold below which values are considered zero. Default is 0.01.}

\item{parallel}{Logical, whether to use parallel processing.}

\item{ncores}{Number of cores to use in the case of parallel computations}

\item{shared_memory}{Logical, when parallel, share the grid with workers via a
filebacked big.matrix (mmap, single machine) instead of serializing column
chunks. Set FALSE for multi-node \code{plan(cluster)} setups. Default TRUE.}

\item{order}{Optional named list mapping a candidate name to the vector of L-moment
orders matched by its optimiser, e.g. \code{list(gengamma = 1:5, expweibull = 1:3)}.
Only the numerically-fitted distributions accept it; passed through to
\code{\link{fitlm_nxts}}. Default NULL.}

\item{...}{Additional arguments to pass to 'nc2xts' function (if 'filename' and 'varname' are provided).}
}
\value{
A list of raster objects containing the fitted distribution parameters, the theoretical L-moments of the fitted distribution,
the sample L-Moments and goodness-of-fit statistics in raster format.
}
\description{
This function fits a list of candidate distributions using the L-moments method NetCDF raster file.
It returns fitted distribution parameters, the theoretical L-moments of the fitted distribution,
the sample L-Moments and goodness-of-fit statistics in raster format.
}
\examples{
TO BE FILLED

}
